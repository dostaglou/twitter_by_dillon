<ul class="nav nav-pills mb-3 d-flex justify-content-between" id="pills-tab" role="tablist">
  <li class="nav-item flex-grow-1">
      <a class="nav-link active text-center" data-toggle="pill" href="#home" aria-controls="pills-home" aria-selected="true">Following</a>
  </li>
  <li class="nav-item flex-grow-1">
      <a class="nav-link text-center" data-toggle="pill" href="#global">Other's</a>
  </li>
  <li class="nav-item flex-grow-1">
      <a class="nav-link text-center" data-toggle="pill" href="#personal">Mine</a>
  </li>
</ul>

<div class="tab-content" id="pills-tabContent">
  <div id="home" class="tab-pane fade show active">
    <% @following_tweets.reverse.each do |tweet| %>
      <%= render 'shared/tweet_box', tweet: tweet %>
    <% end %>
  </div>
  <div id="global" class="tab-pane fade">
    <% @non_personal_tweets.reverse.each do |tweet| %>
      <%= render 'shared/tweet_box', tweet: tweet %>
    <% end %>
  </div>
  <div id="personal" class="tab-pane fade">
    <% @personal_tweets.reverse.each do |tweet| %>
      <%= render 'shared/tweet_box', tweet: tweet %>
    <% end %>
  </div>
</div>

